export type Theme = {
  backgrounds: {
    default: string
  }
  font: {
    colors: {
      primary: string
    }
  }
}

export const theme: Theme = {
  backgrounds: {
    default: "#fafafa",
  },
  font: {
    colors: {
      primary: "#000000",
    },
  },
}
